{
  "name": "private-domain-drive-server",
  "version": "0.1.0",
  "private": true,
  "description": "Private Domain Drive FC server.",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node local.js",
    "dev": "node local.js",
    "test": "node test/identity.test.js && node test/handler.test.js",
    "deploy": "s deploy -y --use-local"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ali-oss": "^6.23.0"
  }
}
